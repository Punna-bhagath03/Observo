import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Observo - All-in-One Observability Platform',
  description: 'Monitor, alert, and investigate incidents in minutes. The unified platform for logs, metrics, traces, and incidents.',
  openGraph: {
    title: 'Observo - All-in-One Observability Platform',
    description: 'Monitor, alert, and investigate incidents in minutes.',
    images: [
      {
        url: 'https://bolt.new/static/og_default.png',
      },
    ],
  },
  twitter: {
    card: 'summary_large_image',
    images: [
      {
        url: 'https://bolt.new/static/og_default.png',
      },
    ],
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
